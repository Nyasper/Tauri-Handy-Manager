name: build-desktop

on:
  push:
    branches: [main]
    tags: ['v*']
  workflow_dispatch:

jobs:
  build:
    strategy:
      fail-fast: false
      matrix:
        include:
          - platform: windows-latest
            bundles: nsis
          - platform: ubuntu-24.04
            bundles: deb,rpm

    runs-on: ${{ matrix.platform }}

    steps:
      - uses: actions/checkout@v4

      - name: Install Linux system dependencies
        if: runner.os == 'Linux'
        run: |
          sudo apt-get update
          sudo apt-get install -y libwebkit2gtk-4.1-dev libappindicator3-dev librsvg2-dev patchelf

      - name: Setup bun
        uses: oven-sh/setup-bun@v2

      - name: Install frontend dependencies
        run: bun install

      - name: Setup Rust toolchain
        uses: dtolnay/rust-toolchain@stable

      - name: Cache Rust artifacts
        uses: swatinem/rust-cache@v2
        with:
          workspaces: ./src-tauri

      - name: Build Tauri bundles
        run: bun run tauri build --bundles ${{ matrix.bundles }}

      - name: Copy portable Windows executable
        if: runner.os == 'Windows'
        shell: pwsh
        run: |
          Copy-Item "src-tauri/target/release/handy-manager.exe" "src-tauri/target/release/handy-manager-portable.exe" -Force

      - name: Upload artifacts
        uses: actions/upload-artifact@v4
        with:
          name: handy-manager-${{ matrix.platform }}
          if-no-files-found: error
          path: |
            src-tauri/target/release/bundle/nsis/*.exe
            src-tauri/target/release/bundle/deb/*.deb
            src-tauri/target/release/bundle/rpm/*.rpm
            src-tauri/target/release/handy-manager-portable.exe

  portable-linux:
    runs-on: ubuntu-22.04

    steps:
      - uses: actions/checkout@v4

      - name: Install Linux system dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y libwebkit2gtk-4.1-dev libappindicator3-dev librsvg2-dev patchelf

      - name: Setup bun
        uses: oven-sh/setup-bun@v2

      - name: Install frontend dependencies
        run: bun install

      - name: Setup Rust toolchain
        uses: dtolnay/rust-toolchain@stable

      - name: Cache Rust artifacts
        uses: swatinem/rust-cache@v2
        with:
          workspaces: ./src-tauri

      - name: Build binary sin bundle
        run: bun run tauri build --no-bundle

      - name: Empaquetar binario portable
        run: |
          strip --strip-unneeded src-tauri/target/release/handy-manager || true
          cp src-tauri/target/release/handy-manager handy-manager-linux-x64

      - name: Verificar glibc minima requerida
        run: |
          echo "Maxima GLIBC requerida:"
          objdump -T handy-manager-linux-x64 | grep -o 'GLIBC_[0-9.]*' | sort -V | tail -1

      - name: Upload portable Linux
        uses: actions/upload-artifact@v4
        with:
          name: handy-manager-linux-portable
          if-no-files-found: error
          path: handy-manager-linux-x64

  release:
    needs: [build, portable-linux]
    if: startsWith(github.ref, 'refs/tags/')
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v4

      - uses: actions/download-artifact@v4
        with:
          path: dist
          pattern: handy-manager-*

      - name: Delete existing release (if any)
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          gh release delete "${{ github.ref_name }}" --yes || true

      - name: Create GitHub Release
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          gh release create "${{ github.ref_name }}" \
            --title "handy-manager ${{ github.ref_name }}" \
            --notes-file CHANGELOG.md \
            $(find dist -type f)
